using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This class and it's children, load all the resources that will be needed for a given seen
// to ensure that they are only loaded once.
public class SceneResources : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
