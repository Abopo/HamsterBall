using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum SIDE { LEFT = 0, RIGHT = 1 };

public class PlatformEndCap : MonoBehaviour {

    public SIDE platformSide; // The side of the end cap that has the platform on it

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
